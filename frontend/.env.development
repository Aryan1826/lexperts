# frontend/.env.development
# Used when: npm run dev (local machine)
# Vite dev server talks directly to local backend

VITE_API_BASE_URL=http://localhost:5001/api/v1
VITE_SERVER_URL=http://localhost:5001
