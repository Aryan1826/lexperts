# frontend/.env.production
# Used when: npm run build (inside Docker)
# Nginx on the same host proxies /api/* → backend container
# So the browser hits relative URLs — no CORS issues at all

VITE_API_BASE_URL=/api/v1
VITE_SERVER_URL=
